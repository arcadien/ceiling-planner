id_format:
  categories: [FUNC, TECH, PERF, UI, CONF]
  levels: ["domain", "feature"]       # e.g. domain=SURFACE, feature=INPUT
  example: "FUNC-SURFACE-INPUT-001"

test_tagging:
  language: python
  convention: "@pytest.mark.req('{id}')"

linter:
  command: "ruff check ."
